# Commands
```
npm init -y
npm install express
npm install dotenv
npm install sequelize
npm install mysql2
npm install nodemon
npm install bcrypt
```


## Execution

```
1. Download zip file from github and extract it.
2. go the project folder and open command prompt at the specified path
3. npm install
4. npm start
```